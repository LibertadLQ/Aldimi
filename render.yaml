services:
  - type: web
    name: aldimi-api
    runtime: python
    buildCommand: bash build.sh
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: ALDIMI_DB_PATH
        value: /tmp/ALDIMI_DB